const canvas=document.getElementById('stars');const ctx=canvas.getContext('2d');let particles=[];const flowers=['🌸','🌺','🌷','❀'];function makeParticle(i){const flower=i%8===0;return{x:Math.random()*innerWidth,y:Math.random()*innerHeight,size:flower?Math.random()*11+10:Math.random()*3.5+1.5,speed:Math.random()*.65+.25,drift:(Math.random()-.5)*.35,alpha:Math.random()*.55+.35,flower,icon:flowers[Math.floor(Math.random()*flowers.length)],rot:Math.random()*Math.PI*2,spin:(Math.random()-.5)*.012}}function resize(){canvas.width=innerWidth;canvas.height=innerHeight;particles=Array.from({length:Math.min(150,Math.max(70,Math.floor(innerWidth/7)))},(_,i)=>makeParticle(i))}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);for(const p of particles){p.y+=p.speed;p.x+=p.drift;p.rot+=p.spin;if(p.y>canvas.height+30){p.y=-30;p.x=Math.random()*canvas.width}if(p.x<-30)p.x=canvas.width+20;if(p.x>canvas.width+30)p.x=-20;ctx.save();ctx.globalAlpha=p.alpha;if(p.flower){ctx.translate(p.x,p.y);ctx.rotate(p.rot);ctx.font=`${p.size}px serif`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(p.icon,0,0)}else{ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fillStyle='rgba(238,247,255,.95)';ctx.shadowColor='rgba(210,232,255,.8)';ctx.shadowBlur=6;ctx.fill()}ctx.restore()}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();

// Decorative flower corners
const decor=document.createElement('div');decor.className='flower-decor';decor.setAttribute('aria-hidden','true');decor.innerHTML='<span class="flower flower-a">🌸</span><span class="flower flower-b">🌷</span><span class="flower flower-c">🌸</span><span class="flower flower-d">🌺</span>';document.body.appendChild(decor);

const audio=document.getElementById('bgMusic');const btn=document.getElementById('musicToggle');btn.addEventListener('click',async()=>{if(audio.paused){try{await audio.play();btn.classList.add('playing');btn.textContent='❚❚'}catch(e){alert('Musik belum bisa diputar. Coba refresh halaman lalu tekan tombol musik lagi.')}}else{audio.pause();btn.classList.remove('playing');btn.textContent='♫'}});